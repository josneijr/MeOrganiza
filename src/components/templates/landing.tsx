import React, { useState } from 'react';
import Logo from 'components/atoms/logo';

const LandingTemplate: React.FC = () => {
    return <div>
        <Logo />
        Landing page bonitona
    </div>
}

export default LandingTemplate;